export interface Service {
    idService: number;
    status: number;
    code: string;
    flyCode: string;
    startDate: string,
    notifiedPassenger: number;
    date: string;
    time: string;
    open: boolean;
}